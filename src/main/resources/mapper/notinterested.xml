    <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
            "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="com.nungil.Repository.Interfaces.NotInterestedRepository">

        <insert id="insertNotInterested">
            INSERT INTO notinterested (user_id, video_id, created_at)
            VALUES (#{userId}, #{videoId}, #{createdAt})
        </insert>

        <delete id="deleteNotInterested">
            DELETE FROM notinterested where user_id = #{userId} AND video_id = #{videoId}
        </delete>

        <select id="existsByUserIdAndVideoId" resultType="boolean">
            SELECT COUNT(1) > 0
            FROM notinterested
            WHERE user_id = #{userId} AND video_id = #{videoId}
        </select>

        <select id="countNotInterestedByUser" resultType="long">
            SELECT COUNT(*) FROM notinterested WHERE user_id = #{userId}
        </select>


    </mapper>
