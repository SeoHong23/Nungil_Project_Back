    <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
            "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="com.nungil.Repository.Interfaces.WatchingRepository">

        <insert id="insertWatching">
            INSERT INTO watching (user_id, video_id, created_at)
            VALUES (#{userId}, #{videoId}, #{createdAt})
        </insert>

        <delete id="deleteWatching">
            DELETE FROM watching where user_id = #{userId} AND video_id = #{videoId}
        </delete>

        <select id="existsByUserIdAndVideoId" resultType="boolean">
            SELECT COUNT(1) > 0
            FROM watching
            WHERE user_id = #{userId} AND video_id = #{videoId}
        </select>

        <select id="countWatchedByUser" resultType="long">
            SELECT COUNT(*) FROM watching WHERE user_id = #{userId}
        </select>


    </mapper>
