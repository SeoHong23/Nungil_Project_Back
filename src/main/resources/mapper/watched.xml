    <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
            "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

    <mapper namespace="com.nungil.Repository.Interfaces.WatchedRepository">

        <insert id="insertWatched">
            INSERT INTO watched (user_id, video_id, created_at)
            VALUES (#{userId}, #{videoId}, #{createdAt})
        </insert>

        <delete id="deleteWatched">
            DELETE FROM watched where user_id = #{userId} AND video_id = #{videoId}
        </delete>

        <select id="existsByUserIdAndVideoId" resultType="boolean">
            SELECT COUNT(1) > 0
            FROM watched
            WHERE user_id = #{userId} AND video_id = #{videoId}
        </select>

        <select id="countWatchedByUser" resultType="long">
            SELECT COUNT(*) FROM watched WHERE user_id = #{userId}
        </select>


    </mapper>
